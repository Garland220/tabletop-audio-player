import { Application, Request, Response } from 'express';

import { Server } from '../';
import { Hash } from '../Shared';
import { Sound } from '../Sounds';


export class SoundController {
    public static ImportJSON(req: Request, res: Response) {

    }
}